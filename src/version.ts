/**
 * Kept in step with the version in package.json. The User-Agent embeds it, so
 * the archives this server reads can tell which build is calling from their own
 * logs.
 */
export const PKG_VERSION = "1.0.0";
export const REPO_URL = "https://github.com/smeet666/mcp-books";
export const ISSUES_URL = "https://github.com/smeet666/mcp-books/issues";
